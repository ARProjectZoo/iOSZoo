
import Foundation
import UIKit
class MyCollectionView {

    /*var arrrayDatos : [UIImage] = []
    var imageInside: UIImage!
    
    init(arrayData : Array){
        self.arrrayDatos = arrayData
    }
    
    func collectionView(_ collectionView: UICollectionView, numberOfItemsInSection section: Int) -> Int {
        return arrrayDatos.count
    }

    func collectionView(_ collectionView: UICollectionView, cellForItemAt       indexPath: IndexPath) -> UICollectionViewCell {
        let cell = collectionView.dequeueReusableCell(withReuseIdentifier:                      "CellsInfo", for: indexPath) as! CellsInfo
        if(indexPath.row == 0){
            cell.imageCells.image = imageInside
        }else{
            cell.imageCells.image = imageInside
        }
        return cell
    }*/
}
